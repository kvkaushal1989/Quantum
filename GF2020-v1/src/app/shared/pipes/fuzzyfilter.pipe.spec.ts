import { FuzzyfilterPipe } from './fuzzyfilter.pipe';

describe('FuzzyfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FuzzyfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
